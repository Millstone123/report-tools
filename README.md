# report-tools

Utilities for generating and validating report themes.

## Usage

    make bootstrap
    make test

## Configuration

Reports use a shared theme profile fetched from the org's theme server.
Set `REPORT_THEME_URL` to override the default endpoint.
